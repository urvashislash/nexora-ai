# Enforce consistent formatting across all Rust toolchain versions.
# This prevents CI drift between local dev and GitHub Actions runners.
edition = "2021"
max_width = 100
tab_spaces = 4
use_field_init_shorthand = true
